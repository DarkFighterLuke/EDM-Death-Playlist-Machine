{% extends "home.html.twig" %}

{% block content %}
    <div>
        <ul><h2>{{ session.username }}</h2></ul>
        <ul><h3>{{ session.email }}</h3></ul>
    </div>

    <div>
        <fieldset>
            <legend><h2>Spotify</h2></legend>
            {{ session.accessToken }}
        </fieldset>
    </div>

{% endblock %}